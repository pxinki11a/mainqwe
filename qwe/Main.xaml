<Page x:Class="qwe.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:qwe"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Main">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" Name="LVMain" Background="#040C0E">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{StaticResource ColorAccent}">
                        <Grid HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Width="100" Text="{Binding namecab}" TextAlignment="Center"/>
                                <TextBlock Width="100" Text="{Binding korpus.name}"  TextAlignment="Center"/>
                                <TextBlock Width="100" Text="{Binding status.name}" TextAlignment="Center" />
                                <TextBlock Width="100" Text="{Binding typecabinet.name}" TextAlignment="Center" />
                                <TextBlock Width="100" Text="{Binding photo}" TextAlignment="Center" />
                                <DockPanel Width="500" >
                                    <Button Width="150" Name="BtnEdit" HorizontalAlignment="Right" Click="BtnCheck_Click" Content="Подробнее"/>
                                    
                                </DockPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
